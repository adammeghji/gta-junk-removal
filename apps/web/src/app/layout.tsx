import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "GTA Junk Removal — Same-Day Service in the Greater Toronto Area",
  description:
    "Book same-day junk removal in the Greater Toronto Area. Fast, affordable, and eco-friendly. Get a free quote in seconds.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
